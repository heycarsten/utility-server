#!/usr/bin/env bash

# Load up .env
set -o allexport
[[ -f .env ]] && source .env
set +o allexport

if [ -z "$IMAGE_TAG" ]; then
  IMAGE_TAG="$1"
fi

if [ -z "$CONTAINER_NAME" ]; then
  CONTAINER_NAME="$2"
fi

if [ -z "$IMAGE_TAG" ] || [ -z "$CONTAINER_NAME" ]; then
  echo 'bin/start - runs the sshd container in the background'
  echo 'usage: bin/start [IMAGE_TAG CONTAINER_NAME]'
  echo
else
  docker run --detach --publish-all --name "$CONTAINER_NAME" "${IMAGE_TAG}:latest"
  docker port "$CONTAINER_NAME" 22
fi
